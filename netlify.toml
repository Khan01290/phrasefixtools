[build]
  publish = "."

# Force lowercase URLs — Netlify will redirect /Bold-Gothic to /bold-gothic automatically
[build.processing]
  skip_processing = false

[[plugins]]
  package = "@netlify/plugin-lighthouse"

# Custom 404 page
[build.environment]
  NODE_VERSION = "18"
